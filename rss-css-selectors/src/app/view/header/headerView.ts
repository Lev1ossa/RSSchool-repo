import { headerProps } from '../../utils/elementsProps';
import { AppView } from '../appView';
import './header.css';

export class HeaderView extends AppView {
  constructor() {
    super(headerProps);
  }
}
